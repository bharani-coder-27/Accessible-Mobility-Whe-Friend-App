app/
│
├── _layout.tsx             # Root layout with AuthProvider & role handling
├── index.tsx                # Default landing (redirect based on auth)
│
├── auth/
│   ├── login.tsx
│   └── register.tsx
│
├── passenger/
│   ├── _layout.tsx          # Passenger layout
│   └── home.tsx
│
├── conductor/
│   ├── _layout.tsx          # Conductor layout
│   └── home.tsx
│
src/
├── contexts/
│   └── AuthContext.tsx
├── services/
│   └── api.ts
├── utils/
│   └── storage.ts
└── types/
    └── auth.ts


/* import React, { useContext } from "react";
import { View, Text, Button } from "react-native";
import { AuthContext } from "../../src/contexts/AuthContext"

export default function PassengerHomeScreen() {
  const { logout, user } = useContext(AuthContext);

  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Welcome Passenger {user?.name}</Text>
      <Button title="Logout" onPress={logout} />
    </View>
  );
} */
